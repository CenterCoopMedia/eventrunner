# Builder c5 report: #196

Branch `joe/confident-gates-gy99ip-b-c5`, worktree `/home/user/wt-c5`, cut from `6a47f70`.
Commits: the feature (#196), this report, then `Regenerate generated output`.

## Issue #196: the unpublished changes page and banner

**What changed.** The admin shell opens one live count of unpublished work: the dirty drafts
(`status == 'dirty'`) of each of the six `_drafts` collections, the predicate `listDirty` and
`cmsPublish { all: true }` use. A banner above the stone states it on every admin screen, and a new
**Unpublished changes** page (Content, last, staff tier, lazy) reads the same value. The page has one
ruled table per collection, **Publish all**, a publish per collection, and **Recent publishes**: the
last 10 `cmsPublishQueue` runs plus every run still `failed` (up to 20), with **Resume publish** on a
failed run. Every action is `cmsPublish`. No endpoint, rule, index, queue, seed, or flag changed.

**Files.**

- New: `apps/web/src/admin/pendingChangesSource.js` (the three list queries),
  `collectionWords.js` (static words and `pendingSentence`), `PendingChangesContext.jsx` (the one
  count), `components/PendingChangesBanner.jsx`, `pendingChanges.js` (page helpers:
  `recordNameOf`, `formatPublishedAt`, `toMillis`, `draftStateOf`, `groupPending`,
  `editorPathFor`, `runSummary`, `mergeRuns`, `summarizeAll`), `pages/AdminUnpublishedChanges.jsx`,
  `e2e/cms-unpublished.spec.js`.
- Shared files, additive: `AdminLayout.jsx` (one docket item, the provider around the shell, the
  banner line before `admin-stone`), `AdminApp.jsx` (one lazy route), `test/setup.js` (one default
  mock of the seam), `AdminLayout.test.jsx`, `AdminApp.test.jsx`, `functions/src/cms/publish.test.cjs`
  (one test), `tests/firestore.rules.test.js` (one describe).
- Docs: `docs/ADMIN_GUIDE.md` (banner sentence after the draft-and-live note, staff row, new
  "Unpublished changes" section after Content), `docs/interface-guidelines.md` and
  `docs/design-reference.md` (one bullet each), `CHANGELOG.md` (one Added bullet),
  `docs/ROADMAP.md` (ticked).

**Tests added.** Web: `collectionWords.test.js` (6), `pendingChanges.test.js` (22),
`pendingChangesSource.test.js` (4), `components/PendingChangesBanner.test.jsx` (12),
`pages/AdminUnpublishedChanges.test.jsx` (14), `AdminLayout.test.jsx` (+1, and the link count is
now `18 + 1`, the base plus one). Functions: `publish.test.cjs` (+1). Rules: 17 (the dirty-drafts
query on each of the six collections, the recent-runs query and the failed-runs query, each allowed
for operator and staff and refused for a non-admin and an anonymous client, plus one that staff get
the seeded dirty draft back). E2E: `cms-unpublished.spec.js` (2).

**Done when: met in full, on the real surface.**

- "saving a block without publishing raises the count": `e2e/cms-unpublished.spec.js` "a saved
  block raises the count on the banner and the page, and a publish from the page clears it". The
  seeded operator signs in through the sign-in page, opens the hero subtitle in the block editor,
  presses **Save draft**, and the banner on that same screen reads "1 unpublished change: 1 content
  block." with `data-pending-total="1"`, drawn above the title band (bounding boxes compared). Web:
  `AdminUnpublishedChanges.test.jsx` "(a) raises the figure and adds the row when a block is
  saved"; `PendingChangesBanner.test.jsx` "follows the count as saves and publishes arrive".
- "publishing clears it": the same E2E test presses **Publish 1 content block** on the page; the
  page reads "Nothing is waiting to be published", the Pages screen shows no banner, and a fresh
  browser context with no admin session reads the new subtitle on `/`. Web: "(b) publishes one
  collection with its ids, and the list clears when the drafts go clean".
- "a failed publish operation is still visible as failed": `e2e/cms-unpublished.spec.js` "a failed
  publish run stays listed as Failed while later runs finish" writes a `cmsPublishQueue` row in the
  shape `publish.cjs` writes when a chunk commit throws, sees it read Failed with its error and
  **Resume publish**, then saves and publishes again through the admin: a new Done run leads and the
  written row still reads Failed. Web: "(c) keeps a failed run listed as Failed with its error and
  one Resume, however many runs follow it" (ten newer Done runs push it out of the recent read; the
  failed read keeps it). Server: `publish.test.cjs` "a failed publish row stays failed, with its
  error, through later publishes, and the failed read finds it". Rules: the failed-runs query is
  pinned for both tiers.
- "the banner and the page never disagree": by construction, one provider in the shell holds the
  count and both only read it. E2E: the first test compares the banner's sentence with the page's
  figure sentence and `data-pending-total`, and the page has exactly one body row, `hero__subtitle`.
  Web: "(d) never lets the figure, the rows, the panel buttons and the banner disagree" checks the
  figure against the row count and each panel button after every emitted snapshot, then follows
  the rail to Pages and back through the banner link.

## Departures from the spec

1. **The proof tint sits on each cell, not on the `<tr>`.** Checked in Chromium through the E2E
   harness: `.admin-proof-row::before` on a `<tr>` is laid out as an extra cell, so every cell moved
   one column right under the heads. On the cells the tint draws as a continuous row. The layer also
   paints over bare text, so every cell's content is an element; the web test pins both (four cells,
   each `admin-proof-row`, none holding a bare text node).
2. **The banner's `mx-auto w-full max-w-admin-canvas` wrapper is inside the banner component**, so
   nothing at all renders when there is no banner. The placement is the spec's: first child of
   `<main>`, before the `admin-stone` div, never inside it (shell test and E2E). The band adds
   `lg:px-lg` so its text lines up with the stone's wide gutter. The in-sentence link takes no
   `admin-target` so it keeps the line's height.
3. **The provider wrap in `AdminLayout.jsx`** is a five-line `AdminLayout` that renders
   `PendingChangesProvider` around the old body, renamed `AdminDesk`, so the shell's JSX is not
   re-indented and the diff stays additive.
4. **Focus after a publish waits for the answer.** When the pressed control leaves with its panel
   while the call runs, focus moves to the result line once the result is in it; the result line is
   `empty:hidden` until then. Web: "moves focus to the result once it arrives when the panel left
   before the answer".
5. Small copy the spec left open: the runs error reads "The publish runs could not be refreshed.
   The list will try again."; `summarizeAll` says "picks it up live" for one change and "picks them
   up live" for more.
6. **Bundle.** The admin entry chunk grew by 3,333 raw and 1,027 to 1,037 gzip bytes (table below).
   About 355 raw bytes of that are the shell wiring the spec's list leaves out (the lazy import and
   its preload entry, the route, the docket item, the provider wrapper), so the four modules are at
   the 3,000 raw cap and under the 1,200 gzip cap. No ceiling was raised.

c4 and c3 are not on this base, so, as the spec says: `COLLECTION_CHOICES` lives in
`collectionWords.js` with the label "Timeline entries", and `recordNameOf`, `formatPublishedAt` and
`toMillis` live in `pendingChanges.js` under c4's names and rules. c4 imports them from there (or
re-exports `COLLECTION_CHOICES`), and aligns the label to c3's "Timeline" if c3 lands first.

## Bundle, before and after

Gzip is zlib level 9, the method `scripts/ci/bundle-budget.cjs` uses.

| Chunk | Before (`6a47f70`) | After |
|---|---|---|
| Admin entry `AdminApp-*.js`, `apps/web/dist` | 126,463 raw, 34,585 gzip | 129,796 raw, 35,612 gzip |
| Admin entry `AdminApp-*.js`, `docs/demo` | 126,463 raw, 34,585 gzip | 129,796 raw, 35,622 gzip |
| `AdminUnpublishedChanges-*.js` (lazy) | none | 11,930 raw, 4,656 gzip |
| Initial chunk, `apps/web/dist` | 1,059,290 raw, 286,733 gzip | 1,059,326 raw, 286,743 gzip |
| Initial chunk, `docs/demo` | 1,069,744 raw, 290,713 gzip | 1,069,780 raw, 290,736 gzip |

Every lazy chunk stays under 180,000 raw and 50,000 gzip; the admin entry chunk has 14,378 gzip
bytes of headroom.

## Decisions the owner may want to change

- No control retires a failed run without resuming it; `cmsUpdatePublishStatus` stays unused. The
  maintenance sweep (every 30 minutes) marks a run with no progress for 90 minutes failed.
- **Resume publish** asks for no confirmation. The failed row says it publishes the current saved
  version of each record the run did not reach, which can include edits made after the failure.
- No per-row publish, no checkbox bulk row, and no sort: the order is newest save first, and the
  panel's button is the bulk action.
- The failed-runs read is `where status == failed, limit 20` with no order (one field, no composite
  index). Past 20 failed runs, which 20 are listed is not defined.
- Speaker pending edits are not counted; the empty state says they are reviewed on each speaker's
  page.
- The run rows do not show the site snapshot (publisher) result.
- The banner has no live role and no dismiss control, and the rail shows no count (spec decision 8;
  it is the admin's own band, not a3's `NoticeBar`).
- The Overview's readiness panel is a read-time snapshot, so it can lag the live banner until
  **Refresh figures**.
- `apps/web/src/components/NoticeBar.jsx` still names issues 196 and 199 as its consumers in a
  comment. #196 does not use it; the comment is left for d1 (#199), which edits that file.

## Final check results

Run last, after the docs and the regenerated output, from this worktree:

| Check | Result |
|---|---|
| `npm run lint` | pass |
| `npm test` | 2,498 tests, 2,498 pass, 0 fail |
| `npm run test -w apps/web` (lock) | 220 files, 2,496 tests pass |
| `npm run test:rules` (lock) | 2 files, 311 tests pass (17 new) |
| `npm run build -w apps/web` (lock) | exit 0 |
| `node scripts/ci/bundle-budget.cjs --dist apps/web/dist` | pass: 1 initial chunk, 1,059,326 raw, 286,743 gzip; every lazy chunk inside its budget |
| `node scripts/generate-content.cjs --demo --check` | 6 files match the committed snapshot |
| `node scripts/build-demo.cjs --check` (lock) | `docs/demo/` matches a fresh build (153 files) |
| `node scripts/ci/bundle-budget.cjs --dist docs/demo` | pass: 1 initial chunk, 1,069,780 raw, 290,736 gzip |
| `node scripts/build-pages.cjs --check` | 29 files and `docs/tokens.css` match |
| `npm run check:copy` | 426 files, no blocked patterns |
| `npm run test:e2e` (lock, Vite cache cleared first) | 33 passed, 0 failed (2 new) |

## Left undone

Nothing in scope.
