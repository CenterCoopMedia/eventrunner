# Builder b4 report: #188, the change request queue

Branch `joe/confident-gates-gy99ip-b-b4`, worktree `/home/user/wt-b4`, on top of `5c2ae53` (a4, a3,
b1, b2, b3 with its review fixes). Spec: `/home/user/specs/b4.md`, built as written except where
listed under "Departures".

Commits, in order:

1. `Read the shell tests' endpoint calls from the mocked admin client`. A fix for two tests that
   fail on the base (see "Found on the base"). It is its own commit so the director can drop it if
   the stack already carries a fix.
2. `Let staff and signed-in visitors send change requests` (Closes #188).
3. This report.
4. `Regenerate generated output`: `apps/web/src/generated/**`, `docs/demo/**`, `docs/docs/**`, and
   the two lockfile integrity lines from the `packages/shared` edit.
5. Seven review-fix commits, one per problem (see "Review round"). As instructed, the review round
   adds no regenerate commit: `docs/demo` and `docs/docs` in commit 4 are stale for these fixes,
   and the director regenerates on the stack. Commit 1 is dropped on the stack, which already
   carries the same `AdminApp.test.jsx` fix; no review fix builds on it.

## What changed

**Server.** `functions/src/admin/changeRequests.cjs` (new), in `functions/index.js` and
`.github/smoke-endpoints.json`:

- `submitChangeRequest { message, page?, submissionKey? }`. The order: 405; the flag, read live from
  `config/features` (off, missing, or not exactly `true`: 404, nothing written; a failed read: 500);
  401 with no token or a sign-in that fails the revocation check (review fix 6); 403 without a
  verified email; 413 over 8 KiB; 400 for the message (1 to 2000
  characters after trim), the page (text, at most 200, blank is null), or a malformed key. Then one
  transaction reads the request and `change_request_rate_limits/{uid}`. A stored request of the same
  account is a replay: 201, nothing written. More than 5 in 15 minutes: 429 with `Retry-After` and
  `retryAfterSeconds`, nothing written. Otherwise it writes the slot, creates the request (`status:
  'new'`, `uid` and lowercased `email` from the token, `createdAt`, `updatedAt: null`, `updatedBy:
  null`), and sets the `admin_logs` row, together. 201 `{ id, ok: true }`.
- `updateChangeRequestStatus { id, status }` and `deleteChangeRequest { id }`: staff tier
  (`requireAdmin(..., { tier: 'staff' })`), not flag gated. Each commits its write and its
  `admin_logs` row in one transaction; 404 for an unknown id writes nothing.
- The audit row is exactly `{ action, docPath, uid, email, at }`, the fields `logAdminAction`
  writes. It never holds the message. `store.cjs` is not edited.

**Rules.** `change_requests`: read `isAdmin()` (either tier), write `false`.
`change_request_rate_limits`: added to the server-only list.

**Flag.** `changeRequests` added at the end of `KNOWN_FEATURE_KEYS`. It is not in
`DEFAULT_ON_FEATURES`, so `buildFeatures` writes it false. Hint: "Let signed-in visitors and staff
send change requests."

**Account delete (b3).** `PER_ACCOUNT_STORES` in `functions/src/users/records.cjs` gains two
stores, both in phase `sweep`:

- `{ kind: 'owned', collection: 'change_requests', field: 'uid', key: 'changeRequests' }`, a new
  kind: every document whose field is the uid, deleted a page of 400 at a time, one batch per page.
- `{ kind: 'doc', collection: 'change_request_rate_limits', key: 'changeRequestLimits' }`: the
  `doc` kind, now also swept (a new `sweepDoc`).

Why `sweep`: an account can hold more requests than one transaction can delete, and a deleted
session's token stays valid for up to an hour and can still send a request. The resume check now
counts both kinds, so a retried delete clears a request sent after the delete instead of answering
404. The delete panel's consequence sentence and the admin guide name the change requests.

**Public site.** `Layout.jsx`: a "Request a change" quiet button after "Share feedback", only when
`features.changeRequests === true` and a user is signed in. The two buttons now sit in one wrapping
row. `ChangeRequestModal.jsx` (new) copies the `FeedbackModal` dialog mechanics and imports its
frame class. `lib/changeRequestApi.js` (new) sends the bearer token and fails soft; in the demo it
says "Change requests are off in this read-only demo."

**Admin.** `AdminChangeRequests.jsx` (new), loaded through `DeferredAdminPage`. Docket: Operations,
last item, `{ to: 'change-requests', label: 'Change requests', tier: 'staff' }`. The page is as the
spec describes: the "Show" filter in `?status=` (Open is the default and leaves the address clean;
an unknown value reads as Open), the form only with the flag on and the notice without it, the count
line, the rows with the next step, Decline, and the `DestructiveConfirm` removal. Since the review
round, every status change and removal states its result under the heading, and focus goes to the
"Requests" heading whenever the pressed control or its row leaves.

**Docs.** `docs/ADMIN_GUIDE.md` (staff row, a Change requests section after Email log, a Features
line, the delete sentence), both handbook pages, `functions/README.md` (the `src/admin/` and
`src/users/` lines, the staff tier row), `CHANGELOG.md`, `docs/ROADMAP.md` (#188 ticked).

## Tests added (relative to the base)

- `functions/src/admin/changeRequests.test.cjs`: 29 (new).
- `functions/src/users/records.test.cjs`: +1, and the seeded fixture, the store-list test, the
  `removed` counts and the paging test now cover the two stores.
- `packages/shared/src/config/schema.test.cjs`: +1. `scripts/lib/answers.test.cjs`: +1.
- `tests/firestore.rules.test.js`: +9 (`change_requests/cr-tier` in `ADMIN_READABLE` gives 4, one
  server-only name, and a "change requests" block of 4).
- Web: `changeRequestApi.test.js` 6, `ChangeRequestModal.test.jsx` 12,
  `AdminChangeRequests.test.jsx` 33 (all new), `Layout.test.jsx` +4, `AdminApp.test.jsx` +1.
  `AdminLayout.test.jsx` asserts 19 links (18 on the base) and `change-requests` as staff.
  `AttendeeRecordPanel.test.jsx` pins the new consequence sentence.
- `e2e/change-requests.spec.js`: 6 (new).

## Done when: met in full, on the real surface

| Clause | Proof |
|---|---|
| "the feature flag is off by default" | E2E `the flag is off by default, and no form is reachable with it off` reads the `config/features` that `init-event.cjs` seeded: `changeRequests` is `false`. Unit: `answers.test.cjs` › `the change request flag is written false by default, and an answers file can turn it on`. The regenerated `eventConfig.js` carries `changeRequests: false`. |
| "and no form is reachable with it off" | The same E2E: a signed-in visitor's footer has no "Request a change", the staff admin page shows the notice and no form, and a direct POST with the visitor's token is 404 with nothing written. E2E `turning the flag off takes the footer control away and refuses the next request at once`. Unit: `changeRequests.test.cjs` › `the flag off, missing, or not exactly true refuses every submission with 404 and writes nothing` and `the flag is read live`; `Layout.test.jsx` › `is absent while the flag is off, for a signed-in reader too`; `AdminChangeRequests.test.jsx` › `with the flag off, there is no form`. |
| "every submission and every status change writes an admin_logs row" | E2E `a visitor sends a request from the footer by keyboard, and it writes an audit row` and `staff find it on the admin page, move it on, and remove it outright; each step writes a row` read the rows back with the Admin SDK: `submitChangeRequest` (the visitor), `updateChangeRequestStatus` and `deleteChangeRequest` (the staff address), none holding the text. Unit: `an accepted request stores the token’s identity ... and writes one audit row`, `status: staff and operator both change a status, and each change writes its row`, `each write and its audit row commit together, in one transaction`, `a failed commit on submit is 500 and leaves the store as it was`, `the audit row has exactly the fields logAdminAction writes`. |
| "an admin can remove a request outright" | The staff E2E removes the request through the real page and confirm; the document is gone and the row leaves the list. Unit: `delete: staff and operator both remove a request outright ...`; `AdminChangeRequests.test.jsx` › `asks first, then calls deleteChangeRequest, says so, and puts focus on the list heading`. |
| "a rate limit test covers the public path" | E2E `the public path is rate limited: the sixth request in 15 minutes is refused` against the real endpoint with the visitor's token (429, nothing stored, no row), and the footer dialog then states the refusal. Unit: `rate limit: the sixth request in 15 minutes is 429 with Retry-After, and writes nothing` (another account is not limited; the window moves on) and `rate limit: a refused request spends no slot`. |

## Review round

The review confirmed 7 problems. Each fix is its own commit, and each new or changed test failed
before the fix.

| # | Problem | Fix (commit) | Tests that failed first |
|---|---|---|---|
| 4 | The busy marker followed the status the control asked for. When the listener delivered the committed status before the HTTP answer, the pressed control lost "Saving…" and `aria-busy` and became the next step, disabled; a pressed Decline, or a row the filter now leaves out, vanished. | `0578ad9` Keep the pressed change request control saving until its call settles. `pending` is `{ id, control: 'next' \| 'decline' \| 'remove' }`; the pressed control keeps "Saving…" until the call settles, and its row stays on screen until then. | `AdminChangeRequests.test.jsx`: `keeps Saving… on the pressed control when the new status arrives before the answer`, `keeps a pressed Decline in place, saving, when the declined status arrives first`. |
| 1 | A status change stated nothing, and focus dropped to the body when the row left the filter or the pressed control went. | `531e6d9` State each change request status change and keep the keyboard on the list. The list's status line says "Request marked done.", "Request declined.", "Request reopened.", or "Request marked in progress."; when the row leaves the filter or the control is Decline, focus moves to the Requests heading (unless the reader already moved it elsewhere); a next-step control that stays keeps focus. | Five cases, `<path>: states the result and moves focus to the list heading` (Mark done and Decline under Open, Mark in progress under New, Reopen under Done, Decline under All); `the same, when the new status arrives before the answer`; `keeps focus on the pressed control when the row and the control stay`. |
| 2 | An open Remove confirm stayed enabled while another row's action ran, and a press did nothing. | `a475037` Disable an open change request confirm while another action runs. Every row's `DestructiveConfirm` takes `busy` while any action runs, so its confirm button is disabled (the component is not edited); only the row being removed reads "Removing…". "Keep it" is disabled for that moment too, because the component ties both buttons to `busy`. | `an open Remove confirm cannot be pressed while another row saves, and says nothing false`. |
| 3 | "Show all requests" removed itself when pressed, and focus dropped to the body. | `75b90f3` Move focus to the list heading when Show all requests is pressed. | `Show all requests moves focus to the list heading, not the page body`. |
| 7 | The Remove consequence said "audit log"; every other surface says "admin log". | `757eaa3` Say admin log, not audit log, in the change request removal. | `asks first, then calls deleteChangeRequest, ...` (pins the new sentence); the E2E pins it too. |
| 5 | The guide said the refusal says when to try again, but it read "Try again later." and no client reads `retryAfterSeconds`. | `ab8a6d2` Name the wait in the change request rate-limit refusal. The server builds "Too many change requests. Try again in 12 minutes." from the window, rounded up, "1 minute" at one; the guide says the refusal names the minutes to wait. | `changeRequests.test.cjs`: `rate limit: the sixth request in 15 minutes is 429 ...` (now pins the message), `rate limit: the refusal names the wait in whole minutes, rounded up, singular at one`. The E2E checks the message against `retryAfterSeconds` and the dialog's alert against the same shape. |
| 6 | `submitChangeRequest` verified the token without the revocation check, so a deleted account's open session could store a request for up to an hour after `deleteAttendee` answered 200. | `a5889c2` Refuse a change request from a deleted account's open session. The endpoint calls `auth.verifyIdToken(token, true)` and answers 401 with nothing written. The guide and `functions/README.md` say so. | `changeRequests.test.cjs`: `a token whose sign-in was deleted is refused with 401 and stores nothing` (also pins that the check is asked for). `records.test.cjs`: `a delete clears the account’s change requests and rate-limit window, and the deleted session cannot send another` (the fake Auth now honors `checkRevoked`). New E2E `a deleted account’s open session cannot send a request`: `deleteAttendee` through the real endpoint, then the visitor's old token gets 401. The Auth emulator refuses a deleted user's token even without the check (the E2E passed with the check removed), so the unit tests are the proof that the endpoint asks for it. |

## Departures from the spec

1. **The rate limit is inside the submit transaction.** The spec had a separate
   `takeChangeRequestRateLimitSlot` transaction before the store transaction. Then a failed commit
   leaves a spent slot, which breaks the spec's own atomicity test ("leaves the store as it was"),
   and every retry of a stored request spends a slot. One transaction now reads the request and the
   window and writes the slot, the request, and the row together.
2. **The flag is read live from `config/features`**, not through `getConfig()`. `getConfig` is the
   five-minute container cache (`core/config.cjs`), so a flag turned off would still admit
   submissions for up to five minutes. A failed read refuses (500). The E2E proves the next request
   after turning the flag off is refused.
3. **A `submissionKey` another account already holds is 409 `conflict`**, nothing written. The spec
   answered 201 for any existing document, which would tell the second sender "We got your request"
   when nothing was stored.
4. **The server test uses the shared `cms/firestoreFake.cjs`.** It already buffers transaction
   writes, has a transactional `create`, and has the `beforeCommit` hook that fails a commit.
5. **E2E added** (the spec said none), as the director asked.
6. Smaller: the dialog's success heading is "Request sent" and focus moves to Close (the send
   control is gone); the message field has a privacy hint ("Only the event team reads your request.
   It is stored with your sign-in address."); status and delete bodies refuse a bad id as
   `id: must be a change request id.`

## Decisions the owner may want to change

- While one row action is in flight, every row's actions are disabled, not only that row's, and
  an open confirm's "Keep it" with them (`DestructiveConfirm` ties both of its buttons to `busy`).
- While a status call runs, its row stays on screen even if the new status leaves the filter.
- Staff requests count against the same 5 per 15 minutes per account.
- A requester cannot read their own requests, and nothing notifies anyone (spec defaults).
- `removed` in the `deleteAttendee` answer gains `changeRequests` and `changeRequestLimits`.
- The public dialog is imported statically, as the spec says. The demo's initial chunk is now
  291,069 of 292,000 gzip bytes (931 left); the next public addition may need the dialog loaded
  with `lazy()`.

## Found on the base, not caused by this branch

- `AdminApp.test.jsx`: the overview and email log tests waited for `fetch`, but the file mocks
  `useAdminApi`, so both failed every run. Commit 1 asserts the mocked call instead.
- `AdminSpeakers.test.jsx` › `offers the soft delete only after the server refuses the full unlink`
  fails in the full web run (a third fetch) and passes alone (29 of 29, three runs). I did not
  touch it.
- E2E `webmcp.spec.js` › `admin tools invoke real diagnostics ...` fails: the seeded operator
  reaches the per-address code limit (5 per 15 minutes) after `email-log` and `event-stats` sign it
  in five times. The suite without `change-requests.spec.js` fails the same way (29 passed, 1
  failed). The new spec signs in two other addresses.

## Not done

- `DestructiveConfirm` moves no focus when its confirm opens (shared, wave 6, not edited). After
  Enter on "Remove", Tab reaches "Remove this request", then "Keep it".
- Google sign-in has no server cap on new accounts, so each Google account gets its own 5 per 15
  minutes (spec §4). The flag is off by default and staff can remove entries.

## Final check results (the last run, after the last review fix)

The run followed the seven review fixes. The three generated-output checks ran against a local
regeneration (`generate-content --demo`, `build-demo`, `build-pages`) that was then discarded,
because this round adds no regenerate commit. Only this report was edited afterwards, and
`npm run lint` and `npm run check:copy` ran again after it, clean.

| Check | Result |
|---|---|
| `npm run lint` | exit 0 |
| `npm test` | 2513 tests, 2513 pass (base 2481, +32) |
| `flock … npm run test -w apps/web` | 217 files, 2462 tests, 2461 pass, 1 fail (the `AdminSpeakers` test above, passes alone); base 214 files, 2406 tests |
| `flock … npm run test:rules` | 2 files, 303 tests, all pass (base 294, +9) |
| `flock … npm run build -w apps/web` | exit 0; `AdminApp` 34.72 kB gzip, `AdminChangeRequests` 3.27 kB gzip |
| `node scripts/ci/bundle-budget.cjs --dist apps/web/dist` | pass: 1 initial chunk, 1,062,308 raw, 287,204 gzip; every lazy chunk under its ceiling |
| `node scripts/generate-content.cjs --demo --check` | 6 files match |
| `flock … node scripts/build-demo.cjs --check` | docs/demo matches a fresh build (152 files), local regeneration |
| `node scripts/ci/bundle-budget.cjs --dist docs/demo` | pass: 1,072,291 raw, 291,079 gzip |
| `node scripts/build-pages.cjs --check` | 29 files and docs/tokens.css match, local regeneration |
| `npm run check:copy` | 424 files, no blocked patterns |
| `flock … npm run test:e2e` | 36 tests: 35 passed, 1 failed (the WebMCP test above); `change-requests.spec.js` 6 of 6 passed |

The first round's last run, for the record: `npm test` 2511, web 2450 (2449 pass), rules 303, E2E
35 (34 pass).
