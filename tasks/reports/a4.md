# Builder a4 report: #186 and #187

Branch `joe/confident-gates-gy99ip-b-a4`, worktree `/home/user/wt-a4`. Commits on top of the base,
in order: the tier split (#186), the Access page (#187), the review fixes for #186, the review fixes
for #187, this report, then `Regenerate generated output`.

## Design

- `config/bootstrap.adminEmails` keeps its meaning and becomes the **operator** list. Every
  existing deployment keeps full access with no migration. `config/bootstrap.staffEmails` is the
  new **staff** list. Any admin is on either list; an operator is on `adminEmails`; an address on
  both is an operator. Both lists are stored lowercase.
- Rules: `isVerifiedSignIn()`, `bootstrapAllowlist(field)` (reads the field with a `[]` default so
  a pre-split document works), `isOperator()`, `isStaff()`, `isAdmin()` = either. Every
  admin-readable collection reads on `isAdmin()`; `admin_logs` reads on `isOperator()`.
- Server: `functions/src/core/auth.cjs` exports `ADMIN_TIERS`, `resolveAdminTier(bootstrap,
  email)`, `loadBootstrap`, `BootstrapUnavailableError`, `BOOTSTRAP_UNAVAILABLE` and
  `requireAdmin(deps, req, { tier })`. The option defaults to `'operator'`, so an endpoint that
  does not state a tier refuses staff. Every existing `requireAdmin` call states its tier at the
  call and passes `db`. With `db` the gate reads `config/bootstrap` live and **fails closed**: a
  read that throws answers 500 and admits nobody; an absent document means no admins; the cached
  copy is never consulted. Only `requireAttendeeAccess` (no `db`) reads the five-minute cache, on
  purpose. The three hand-rolled `adminEmails` scans (`speakers/profile.cjs`, `media/upload.cjs`
  twice) use `resolveAdminTier` over the same live read; `materials/actor.cjs` surfaces a failed
  read as 500 on a write and as not-admin on the optional read.
- `config/event.sender` is the operator's to **change**: `admin/config.cjs` compares a sent sender
  with the stored one inside the write transaction (email and reply-to case-insensitive, name
  trimmed, empty as null) and refuses a staff save only when a value differs. The Event form does
  not send the block for staff at all and shows the three fields read-only with a one-line note.
- Branding is the operator's in the media library too: `mediaUpload` into `branding/`,
  `mediaDelete` and `mediaUpdateMetadata` on a branding asset (folder or `branding/` path), and
  `mediaDelete` of any asset a `config/theme` slot references, force or not, gate at staff and then
  ask for the operator tier. A staff delete whose usage scan fails is refused even with force.
- Client: `AuthContext` runs a second rules probe (`admin_logs`, limit 1) beside the drafts probe
  and exposes `adminTier` (`'operator' | 'staff' | null`) and `isOperator`; `adminStatus` stays
  `'unknown'` until both probes answer. `AdminLayout.jsx` is the one place a page declares its
  tier: every `DOCKET` item carries `tier`, `docketForTier` hides the rest, and
  `sectionTier(pathname)` (percent-decoded, lowercased, the way the router matches; a path no
  staff section owns is the operator's) plus `tierReaches` render a refusal in place of `<Outlet />`
  for an out-of-tier route. `TIER_SCOPE` names each tier's sections from the docket in the rail's
  own words for the refusal, the Access page's description and its confirmation sentences.
- WebMCP: each admin tool declares a tier mirroring its endpoint; `adminToolsForTier` mounts the
  set the tier holds.
- Access page (#187): `functions/src/admin/access.cjs` with `listAdminAccess` and `setAdminAccess`
  (both operator-only, both read `config/bootstrap` fresh). `setAdminAccess { email, tier:
  operator|staff|none }` lowercases, moves the address between the lists in a transaction, refuses
  409 `last-operator` when the result would have no operator (the caller's own grant included),
  answers `changed: false` without a write for a no-op, writes an `admin_logs` row through the
  shared `logAdminAction` (optional `details`: `{ email, tier, previousTier }`), and refreshes its
  own container's config cache. The page is one ruled table with the tier as a badge word, the
  caller's row marked "(you)", quiet row actions, a grant form, and one still confirmation surface
  under the grant form for every change; focus moves in effects after render (onto the surface,
  back to the trigger or the grant field, onto a refusal); an already-held tier is stated in place
  with no confirmation; a failed first load is an error state with one retry.
- `init-event.cjs` takes `--staff`; the answers file takes `staffEmails`. On a re-run against an
  existing `config/bootstrap` the answers file's lists are not re-applied: only explicit `--admin`
  and `--staff` add, the merge stays additive, and the output names what was added. The readiness
  "two admins" row counts operators only.

## Classification

Rules, admin-readable collections (all in the `ADMIN_READABLE` matrix in
`tests/firestore.rules.test.js`):

| Collection | Tier |
|---|---|
| The six publishable collections and their `_drafts` (hidden live docs, drafts) | admin (either) |
| `cmsVersionHistory`, `cmsPublishQueue` | admin |
| `media_assets`, `users`, `users_public` (admin branch), `speakers`, `feedback` | admin |
| `admin_logs` | operator |
| `config/bootstrap` | nobody (server-only for both tiers) |

Server endpoints (the tier each `requireAdmin` call states):

| Tier | Endpoints |
|---|---|
| operator | `updateFeatures`, `updateTheme`, `listAdminAccess`, `setAdminAccess`, `listSystemErrors`, `resolveSystemErrors`, `webMcpInspectSystemErrors` |
| staff, with an operator-only part | `updateEventConfig` (a change to `sender`), `mediaUpload` (folder `branding`), `mediaDelete` and `mediaUpdateMetadata` (a branding asset; for delete also any asset `config/theme` references) |
| staff | `updateBadges`, `cmsCreateContent`, `cmsUpdateContent`, `cmsDeleteContent`, `cmsSavePage`, `cmsDeletePage`, `cmsSaveUpdate`, `cmsDeleteUpdate`, `cmsPublish`, `cmsGetPublishQueue`, `cmsUpdatePublishStatus`, `cmsGetVersionHistory`, `scanMediaUsage`, `speakerPhotoUpload`/`speakerPhotoDelete` (admin path), `createSpeaker`, `updateSpeaker`, `deleteSpeaker`, `applySpeakerPendingEdits`, `discardSpeakerPendingEdits`, `getOwnSpeakerProfile`/`updateOwnSpeakerProfile` (admin path), `sendSpeakerInvite`, `resendSpeakerInvite`, `cancelSpeakerInvite`, `listSpeakerInvites`, `approveUser`, `revokeUser`, `removeUserCustomBadge`, `saveLiveUpdate`, `deleteLiveUpdate`, `updateFeedbackStatus`, `setMaterialReviewStatus`, the materials store and access endpoints (via `resolveActor`), `getTicketingStatus`, `ticketingSync`, `ticketingImportCsv`, `ticketingListTickets`, `createUserFromTicket`, `webMcpCheckEventReadiness`, `webMcpValidateCurrentPageDraft`, `webMcpInspectPublishQueue`, `webMcpCheckMediaUsage`, `webMcpCheckTicketingHealth` |

Docket sections: Pages, Sessions, Content, Media, Materials, Speakers, Attendees, Badges, Live
updates, Ticketing, Feedback, Event = staff; Features, Branding, Access, System errors = operator.

## The review round

The director's five-lens review confirmed 17 findings. Every one is fixed, test first, in the two
review-fix commits; none was judged wrong.

| # | Finding | Fix |
|---|---|---|
| 1 | Staff could never save Event settings: the form always sent `sender` and the server refused any staff payload carrying the key. | Server compares the sent sender with the stored one inside the transaction and refuses a staff CHANGE only (`senderChanged`, `findOperatorKeyChanges`). Client leaves `sender` out of a staff payload and draws the fields read-only with a note. Tests: the whole `toPayload` shape with sender unchanged saves for staff (server and page); a change to name or reply-to is refused; case and whitespace do not count as a change. |
| 2 | Staff could change branding through the media library (`mediaDelete` with `force`, branding uploads, relabels). | `mediaUpload` into `branding/`, `mediaDelete`/`mediaUpdateMetadata` on a branding asset, and `mediaDelete` of any asset `config/theme` references now gate at staff and then require the operator tier (`isBrandingAsset`, and the reference predicate now named `referencedByBranding`, see round 3). A staff delete whose scan fails is refused even with force. Tests for staff refused and operator allowed on each; E2E asserts the branding upload refusal. |
| 3 | Access page focus fell to the body after every confirmed change and after a refusal. | Focus moves in effects after render: onto the surface when it opens, back to the trigger (or the grant field when its row is gone) when the pending change clears, onto the refusal when it arrives. Tests read `document.activeElement` on each path. |
| 4 | An invalid grant address marked the field but did not focus it. | `focusFirstError` after the render that marks it. Test. |
| 5 | A grant to an address already holding that tier stated a false consequence. | Said in place ("x already has staff access.") with no confirmation. Test. |
| 6 | Re-running init re-added addresses an operator revoked or demoted. | On a re-run against an existing bootstrap the answers file's lists are not re-applied; only `--admin`/`--staff` add (`bootstrapAdditions`), and the output names what was added. Tests in `write.test` and `init-event.test`; documented in `scripts/README.md` and `DEPLOY_RUNBOOK.md`. |
| 7 | `loadBootstrap` fell back to the cached copy on a failed read or an absent document. | With a `db` it fails closed: a throwing read is a 500 that admits nobody; an absent document means no admins. The cached path stays only for callers with no `db`. Every function test fixture now seeds `config/bootstrap` (its handlers read it live), and the "not an admin" negatives use a stranger token instead of an emptied config. |
| 8 | The client refusal was bypassed by case or percent-encoding in the path. | `sectionTier` decodes and lowercases the segment and fails closed on a path no staff section owns. Tests for `/admin/Branding` and `/admin/%41ccess`, unit and shell. (Round 3 widened this to the whole pathname; see below.) |
| 9 | The "waits for the tier probe" test hung the wrong probe. | It hangs the `admin_logs` probe while the drafts probe resolves, asserts "Checking your access…" and no docket, then resolves and asserts the operator docket. |
| 10 | Every result and refusal was announced twice. | The toasts pass `{ announce: false }`; the in-place notice is the announcement. Tests assert the option. |
| 11 | A failed first load drew "No admin accounts" and counts, and a grant guessed the standing. | Error state with one "Try again"; no empty state or counts; the confirmation for an unknown standing is worded neutrally ("Set staff access for x"). Test. |
| 12 | Tier scopes were named in different words, and the staff copy left out Event. | `TIER_SCOPE` in `AdminLayout.jsx`, derived from the docket, used by the description, the consequence sentences and `TierRefusal`; "the deployment settings" removed. Tests pin the strings. |
| 13 | No `aria-busy` on the confirm button. | Added; test. |
| 14 | Colon capitalization in the admin guide. | Fixed. |
| 15 | Docs and code said removing "the only other operator" is refused. | Now: demoting or removing the last operator, your own grant included; test renamed. |
| 16 | Docs said the confirmation opens under the table. | Under the grant form, above the table, as the page draws it. |
| 17 | The shared-package lockfile step. | `npm run prepare:functions && rm -rf functions/node_modules/shared && npm install` ran; the two lockfile changes are in the regenerate commit. |

## The second review round

A second adversarial review, of the fix commits and of the whole tier model, confirmed eleven more
findings. Every one is fixed, test first, in two more commits (one per issue); the security findings
take the safer fix. None was judged wrong.

| # | Finding | Fix |
|---|---|---|
| 1 | `config/event.seo.defaultOgImagePath` is the social sharing card, a branding surface `public/og.cjs` renders right after `theme.logos.ogDefault`, and staff could point it anywhere through Event settings. `mediaDelete` did not count a reference there as branding. | `findOperatorKeyChanges` compares the card path with the stored one inside the transaction (`ogImageChanged`) and refuses a staff change as `seo.defaultOgImagePath: operator access required`; a `seo` value that is not an object counts as a change when a card is stored. `referencedByBranding` (renamed from `referencedByTheme`) counts a `config/event` reference at field `seo.defaultOgImagePath` beside any `config/theme` slot, for `mediaDelete` and now `mediaUpdateMetadata`. Tests: staff refused, unchanged and omitted go through and keep the stored value, operator allowed; delete refused for staff with force and allowed for an operator; a `venue.mapPath` reference is not branding. E2E asserts the card refusal on the real endpoint. |
| 2 | The admin speaker paths took any `headshotPath`, and `applySpeakerPendingEdits` deleted whatever object the old path named — a way to delete a branding file or another speaker's photo. | `applyCreateSpeaker` and `applyUpdateSpeaker` validate with `isOwnHeadshotPath(id, path)` (own `speaker-photos/{id}/` prefix, `default-avatars/`, or null), create against the id the record will carry (the slug when none is sent). `applyApplySpeakerPendingEdits` only ever deletes an object under `speaker-photos/{speakerId}/`. Tests: both admin paths refuse by name and write nothing, own prefix and avatar accepted, handler returns the refusal verbatim, a stale `branding/`, other-speaker or avatar path survives the apply. |
| 3 | A staff sender judged unchanged was still merged from the payload, so the stored bytes could be re-spelt; an unknown sender key slipped through; `domainVerifiedBy`/`domainVerifiedDomain` were writable. | `withoutUnchangedOperatorKeys` drops `sender` (and the card path) from a staff payload after the comparison passes, so the stored bytes are kept. Any sender key outside `email`, `name`, `replyTo` is refused as `sender.<key>: operator access required`. `SENDER_VERIFICATION_FIELDS` is the whole record verify-sender-domain.cjs writes, read-only for both tiers; init-event's `PRESERVED_PATHS` preserves the two new fields on `--force`. Tests each. |
| 4 | The Media page offered staff the Branding drawer's write controls and then showed the server's raw refusal. | `MediaLibrary` reads `adminTier`: for staff the Branding drawer has no Upload and carries "An operator manages branding files."; `AssetModal` takes `operator` (default closed) and for a branding asset, or one the scan shows on a theme slot or the card, draws alt and title read-only with no Save description and no Delete. `mediaErrors.js` maps `branding: operator access required` to "Only an operator can change branding files." in the upload, save and delete paths. Tests for staff and operator on both drawers, the scan-locked page image, and all three mappings. |
| 5 | `mediaUpdateMetadata` let staff relabel an asset a theme slot uses. | Same usage scan as `mediaDelete` for staff; a theme or card reference is refused; a scan that fails is a 500 ("The asset could not be checked for usage.") for staff and no write; an operator is not scanned. Tests. |
| 6 | The client tier refusal was bypassed by case or encoding of the `/admin` prefix (`/Admin/branding`, `/%41dmin/…`). | `sectionTier` decodes and lowercases every segment of the whole pathname; a segment that decodes to a slash, an empty segment before more path, or a first segment that is not `admin` is the operator's. Tests for `/Admin/branding`, `/ADMIN/Branding`, `/%41dmin/%42randing`, `/Admin/pages`, `/Admin`, `/admin/%2Fbranding`, `/admin//branding`, unit and shell. |
| 7 | "Try again" unmounted when pressed, dropping focus. | The control stays mounted through the retry, `aria-busy` with the label "Loading…", and takes the focus back when the retry fails; when the list it asked for lands, focus moves to the table (`tabIndex={-1}`). Test with a held-open retry that is then failed. |
| 8 | The focus effect keyed on `accounts` pulled focus to the surface when a list load landed. | The effect depends on `pending` alone, so it runs only when the pending change opens, closes or is replaced. Test: a list that lands while a confirmation is open leaves the keyboard where it was. |
| 9 | The refusal-focus test passed without the fix. | It focuses the confirm button before pressing it and asserts `document.activeElement` is exactly the error container. |
| 10 | Missing tests. | Staff `mediaDelete` with force while the scan fails (500, nothing deleted; operator 200); `config/bootstrap` reads that throw answer 500 `internal` and write nothing in `createSpeaker`, the speaker self-or-admin gate, `speakerPhotoUpload` and `speakerPhotoDelete`. |
| 11 | Copy and docs. | "at once and can open every section, including …" / "at once and can run …", pinned in the failed-first-load and vocabulary tests. The read-only Sender panel reads "The From address every transactional email uses. You can read it here. An operator changes it." `ADMIN_GUIDE.md` names the three sender fields staff can read and cannot change and the card path; `CHANGELOG.md` and `functions/README.md` match the code (the staff row, the card path, the scan rule, the headshot prefix rule). |

### The model, walked once more

Every server path by which a stored value reaches a branding surface (logos, favicon, social
card, app icons, theme) or a sender identity, and what holds it:

| Surface or identity | Stored where | Server writers | Holding |
|---|---|---|---|
| Theme: colours, logos (primary, mark, favicon, app icons, `ogDefault`) | `config/theme` | `updateTheme` (`admin/config.cjs`); `init-event.cjs` seed | Operator (`CONFIG_DOC_TIERS.theme`); init is an operator script. Client writes: `allow write: if false`. |
| Social sharing card | `config/event.seo.defaultOgImagePath`, read by `public/og.cjs` after `theme.logos.ogDefault` | `updateEventConfig`; `init-event.cjs` seed | Compared in the transaction; a staff change refused by name; unchanged dropped from the merge. |
| Branding objects | Storage `branding/**` | `mediaUpload` (write), `mediaDelete` (delete), `applySpeakerPendingEdits` (delete of an old headshot), `speakerPhotoDelete` | Upload into `branding/` operator-only; delete of a branding asset or any asset a theme slot or the card references operator-only, force or not, and refused when the scan fails; the pending-edits delete only reaches `speaker-photos/{speakerId}/`; `speakerPhotoDelete` is prefix-validated. `storage.rules` deny client writes. |
| Branding words (alt, title) | `media_assets` rows | `mediaUpdateMetadata` | Operator-only for a branding asset or one on a theme slot or the card; scan failure refuses staff. Client writes: `if false`. |
| Speaker headshot on the public page | `speakers.headshotPath` | `applyCreateSpeaker`, `applyUpdateSpeaker`, `applyUpdateOwnSpeakerProfile` (direct or staged), `speakerPhotoUpload`/`Delete` | Every path validates `isOwnHeadshotPath`; the photo endpoints write their own generated path under the prefix. Client writes: `if false`. |
| Attendee photo | `users.photoPath` | The attendee's own upload | Not a branding surface; attendee-owned. |
| Sender identity (From, name, reply-to) | `config/event.sender`, read by `email/send.cjs`, `email/render.cjs`, the speaker invite template | `updateEventConfig`; `scripts/verify-sender-domain.cjs`; `init-event.cjs` | Compared in the transaction (email, name, replyTo; any other key is a change); unchanged dropped from the merge; `domainVerified`, `domainVerifiedAt`, `domainVerifiedBy`, `domainVerifiedDomain` read-only for both tiers and preserved by init's `--force`. |
| Email body and subject | `email_templates` | `init-event.cjs` only | No admin endpoint writes it; client read and write `if false`. |
| Deploy-time branding (manifest, tokens.css) | Built from committed data by `scripts/build-pages.cjs` and `site-manifest` | Build scripts | Not reachable from an admin request. |

Result: every path is operator-only or compared, and each is pinned by a test that a staff caller is
refused and an operator allowed. `requireAttendeeAccess` still reads the cached bootstrap on
purpose (see "Left undone").

## The connector review (PR #269)

Four findings, each confirmed against the code, fixed test-first in two commits after the report
commit (`9330433` for issue 186, `069f0a3` for issue 187; no regenerate commit — the director
regenerates on the stack).

| # | Finding | Fix |
|---|---|---|
| 1 | An operator who demoted or revoked their OWN account kept the operator rail: the list reload 403'd and was kept as "last values", and AuthContext probed only when `user` changed. | `AuthContext` exposes `refreshAdminStatus()`, which re-runs both probes for the same user and keeps the current verdict until the new one lands. The Access page calls it after a successful change whose address is the caller's (`callerEmail` or the signed-in address, lowercased). A self-demotion refuses the Access route and draws the staff rail at once; a self-revocation shows the not-an-admin state. Shell tests drive both through the page; a unit test pins that only a change to the caller's own address re-probes. |
| 2 | Every rejected `admin_logs` read made the caller staff for the session, with no retry — a transient failure demoted an operator. | Staff is classified only on `code === 'permission-denied'`. Any other rejection leaves `adminTier` `'unknown'`: the rail draws the sections every admin holds, refuses no section on a guess (the server decides), says "Your access tier could not be checked." and offers "Check again", which re-probes. Shell tests: an `unavailable` rejection renders Branding without refusal and no tier word; the retry shows Operator and the Access link. The test fakes now reject with a real `permission-denied` code for staff. |
| 3 | Records written before the prefix rule hold headshot paths such as `speakers/rae.jpg`, and the admin editor resends the current `headshotPath` on every save, so every edit of such a record failed after round 3. | `applyUpdateSpeaker` compares inside the transaction against the stored record: the stored value is always allowed back; only a CHANGE to a path outside `speaker-photos/{speakerId}/` (or `default-avatars/`, or null) is refused by name. Create is unchanged (a new record has no stored path). Pending-edits deletion stays limited to the speaker's own prefix. Tests: an unrelated edit carrying the legacy path succeeds; moving it to another foreign path is refused and nothing is written; moving it under the own prefix succeeds. |
| 4 | When the account already held the requested tier but the stored lists needed normalizing (case, whitespace, duplicates, order), the transaction rewrote `config/bootstrap` but answered `changed: false`, so no audit row was written — a mixed-case entry newly matched by the rules was an unaudited effective grant. | `applyAccessChange` returns the actual list change as `changed` beside `tierChanged`; the handler writes the `admin_logs` row whenever the stored lists changed, with `normalized: true` in the details when the tier stood, refreshes the cache as for any change, and answers the real result. The page says "already had this access" for such a write (the server's `previousTier` wins over the page's memory) rather than claiming a new tier. Tests: server (row, details, refresh, normalized lists, response) and page (sentence). |

## Decisions the owner may want to change

- **Event settings are staff**, with the sender held back only on change. Badges are staff.
  Ticketing operations are staff (provider setup lives in Tier A env and operator scripts).
- **`admin_logs` is operator-only** and doubles as the read the browser probes to learn its tier.
  If a later page needs staff to read it, the tier probe needs another operator-only read.
- **Fail-closed on an absent `config/bootstrap`** with a `db`: a deployment whose bootstrap
  document is deleted has no admins until it is written again (init-event, or the Admin SDK).
- **A path no staff section owns is the operator's** in the shell, so `/admin/nope` shows the tier
  refusal to staff rather than "Admin page not found". The server was never affected.
- **A grant that changes neither the tier nor the stored lists writes no `admin_logs` row**
  (`changed: false`); one that only normalizes the stored lists is written and logged as a
  normalization (connector review). `approveUser` logs no-ops.
- Storage rules do not check admin at all (they cannot read Firestore), so `storage.rules` and its
  tests are unchanged.
- The review fixes are two commits (one per issue) after the two feature commits rather than
  amended into them, so the review's findings and their fixes are readable as a diff.

## Issue #186: what changed

Files: `firestore.rules`, `tests/firestore.rules.test.js`, `functions/src/core/auth.cjs` (+ test),
every module with a `requireAdmin` call (see classification) and every function test fixture,
`functions/src/admin/config.cjs` (+ test), `functions/src/admin/webMcpDiagnostics.cjs` (+ test),
`functions/src/telemetry/systemErrorsAdmin.cjs` (+ test), `functions/src/materials/actor.cjs`
(+ new test), `functions/src/media/upload.cjs` and `metadata.cjs` (+ tests),
`functions/src/speakers/profile.cjs` (+ test), `functions/src/users/approval.cjs` (+ test),
`functions/src/cms/content.cjs` (+ test), `apps/web/src/contexts/AuthContext.jsx`,
`apps/web/src/admin/AdminLayout.jsx` (+ test), `apps/web/src/admin/AdminApp.jsx` (+ test),
`apps/web/src/admin/pages/AdminEventSettings.jsx` (+ `AdminSettings.test.jsx`),
`apps/web/src/webmcp/adminTools.js` (+ test), `apps/web/src/webmcp/AdminWebMcpRegistration.jsx`,
`scripts/init-event.cjs` (+ test), `scripts/lib/answers.cjs` (+ test), `scripts/lib/idempotency.cjs`
(+ test), `scripts/lib/write.cjs` (+ test), `scripts/lib/checklist.cjs`, `scripts/lib/emit.test.cjs`,
`packages/shared/src/readiness.cjs` (+ test), `e2e/admin-access.spec.js`, docs:
`functions/README.md`, `docs/ADMIN_GUIDE.md`, `docs/DEPLOY_RUNBOOK.md`, `docs/CLIENT_ONBOARDING.md`,
`scripts/README.md`, `apps/web/README.md`, `docs/operator/webmcp-diagnostics.md`,
`docs/handbook/for-event-staff.md`, `CHANGELOG.md`, `docs/ROADMAP.md`.

Done when: **met, on the real surface.** The E2E spec signs a staff account into the browser and
checks the staff rail with `/admin/branding` refused; at the HTTP layer it proves staff are refused
`updateTheme`, `updateFeatures`, `listAdminAccess`, `setAdminAccess`, `listSystemErrors`, a
`sender` change and a branding upload (all 403), and keep `cmsGetVersionHistory`,
`listSpeakerInvites`, a full event save with the sender unchanged and a `cms-images` upload (all
200). The rules matrix covers both tiers on every admin-readable collection, and a staff branding
write is refused by the rules (no browser writes config).

## Issue #187: what changed

Files: `functions/src/admin/access.cjs` (+ test), `functions/src/cms/store.cjs` (`details` on
`logAdminAction`), `functions/index.js`, `.github/smoke-endpoints.json`,
`apps/web/src/admin/pages/AdminAccess.jsx` (+ test), `apps/web/src/admin/AdminApp.jsx` (route),
`e2e/admin-access.spec.js`, `docs/ADMIN_GUIDE.md` ("Settings → Access"), `functions/README.md`,
`CHANGELOG.md`.

Done when: **met, on the real surface.** In the E2E spec the seeded operator grants staff access
(lowercased), the `admin_logs` row with `details` is asserted, the new account reaches the content
endpoints and only those, signs into the browser and sees the staff rail, and the last-operator
refusal and a revocation are exercised.

## Final check results (last run, after docs and generated output)

Every check below ran after the last edit to any file it reads: the four review-fix commits were
made, the shared lockfile step re-ran, then `docs/docs`, `docs/demo` and `apps/web/src/generated`
were regenerated, then the whole set ran in sequence under the heavy lock (third round). Only this
report was edited afterwards, and `npm run lint` and `npm run check:copy` (the two checks that read
Markdown) ran again after it, clean. The regenerated output and the two lockfile integrity changes
are committed in the `Regenerate generated output` commit that follows the report.

| Check | Result |
|---|---|
| `npm run lint` | clean, exit 0 |
| `npm test` (shared, functions, scripts) | 2263 tests, 2263 pass, 0 fail |
| `flock … npm run test -w apps/web` | 185 files, 2121 tests, all pass |
| `flock … npm run test:rules` | 2 files (Firestore, Storage), 282 tests, all pass |
| `flock … npm run build -w apps/web` | built, exit 0 (main chunk 1,058.94 kB / 285.86 kB gzip) |
| `node scripts/generate-content.cjs --demo --check` | 6 files match the committed snapshot |
| `flock … node scripts/build-demo.cjs --check` | docs/demo matches a fresh build (139 files) |
| `node scripts/build-pages.cjs --check` | 28 files and docs/tokens.css match the committed site |
| `npm run check:copy` | 377 files, no blocked patterns |
| `flock … npm run test:e2e` | 17 passed (6 in `admin-access.spec.js`), 0 failed |

After the connector-review commits (`9330433`, `069f0a3`) the checks the director asked for ran
again, in sequence under the lock, on the working tree those commits left; only this report was
edited afterwards, and `npm run lint` ran again after it, clean. No generated output changed (the
director regenerates on the stack).

| Check (connector round) | Result |
|---|---|
| `npm run lint` | clean, exit 0 |
| `npm test` (shared, functions, scripts) | 2266 tests, 2266 pass, 0 fail |
| `flock … npm run test -w apps/web` | 185 files, 2126 tests, all pass |
| `flock … npm run test:rules` | 2 files, 282 tests, all pass |
| `flock … npm run test:e2e` | 17 passed, 0 failed |

The E2E suite ran in this sandbox against the emulators (Java 21, Chromium from
`PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers`). Its first run in the first round caught the
five-minute cache admitting and refusing the wrong accounts, which is why `requireAdmin` reads
`config/bootstrap` live; the spec also proves on the real surface that a staff save with the
sender unchanged goes through, and that a staff sender change, a staff social-card change and a
staff branding upload are each refused by name.

## Left undone

- Nothing from the two issues or the two review rounds. Two notes outside their scope:
  - The tier probe and `admin_logs` are coupled (see decisions).
  - `requireAttendeeAccess` (bookmarks, reactions) still reads the cached bootstrap on purpose;
    a revoked admin who is not also an approved attendee keeps attendee access for up to five
    minutes. It runs at attendee volume, and its admin branch only widens what an approved
    attendee already has.
