# Builder a3: design vocabulary wave 2 (#249) and #234

Branch `joe/confident-gates-gy99ip-b-a3`. This file is updated after each
device family lands, so the work survives a lost context.

## Inventory before the wave

Built in wave 1 (#254): the state grammar, the motion grammar and its test,
switch, segmented control, tabs, checkbox, radio, search field, sort control,
filter group, external link marker, toast tones, dialog, the specimen book and
its capture script.

Built by M8 (2026-09-11) or earlier, and how each stands against §3 of the
record:

| Device | Existed as | Gap against §3 |
|---|---|---|
| Count | `components/session/SavedCount.jsx` (labelled figure, data face, tabular figures) | No shared device, no contract, no specimen |
| Legend | One paragraph in `pages/Schedule.jsx` | No shared device, no contract, no specimen |
| Session state marker | `components/session/SessionState.jsx`, drawn as a `Tag` | No ink change; a finished session does not take the back-issue ink; no contract |
| Loading | `components/LoadingState.jsx` | Meets the rule |
| Empty | `components/EmptyState.jsx` | Meets the rule |
| Callout | `components/editorial/Callout.jsx` | Contract and stylesheet, no call site |
| Avatar | `components/media/ProfilePhoto.jsx` (square, brand radius, initial) | Reads the `brand-*` names, no contract, no specimen |
| Standfirst | The `text-lead text-text-secondary` line under a heading, written by hand in four places | No device, no contract |

Not built anywhere: definition list, timeline (the updates feed draws a spine
in CSS but no device exists), ruled table, progress, notice bar, dropzone,
repeater, figure sentence, pull quote, byline and dateline, drop cap, and the
§4 option groups.

## What landed

### Family 1: the definition list, the fact block (#234), and the pull quote

- `design/tokens/components.json`: `definition-list` and `pull-quote`
  contracts. Every preset remaps both (`design/tokens/presets/*.json`), and
  the catalog mirrors were regenerated (`packages/shared/src/presetCatalog.cjs`,
  `design/tokens/presets/README.md`).
- `apps/web/src/index.css`: the `.definition-list__*` and `.pull-quote*` rules.
- `apps/web/src/components/editorial/DefinitionList.jsx` (+ `DefinitionPair`),
  `PullQuote.jsx`, and tests that render both in every style and both modes
  (`apps/web/src/test/everyStyle.jsx` is the shared helper).
- `apps/web/src/components/blocks/FactBlock.jsx` and `QuoteBlock.jsx`, with
  the registry on both sides (`functions/src/cms/blockTypes.cjs`,
  `apps/web/src/admin/blockTypes.js`), the renderer registry, `SectionBlocks`
  (a run of facts is one `<dl>`), `blockSearchText`, the editor hints, and
  tests beside each.
- `InfoCards.jsx`: a fact opens a card, drawn through the definition list.
- `scripts/lib/seed.cjs`: the key facts section seeds three facts (when, where,
  who); when and where come from `config/event`; `info__where_venue` and
  `info__where_address` are obsolete ids; the details section admits a quote.
  `scripts/lib/demo-event.cjs`: the fixture carries the three facts and one
  quote.
- `docs/ADMIN_GUIDE.md`, `docs/design-reference.md`, `CHANGELOG.md`.
- Specimen book: pull quote, definition list, and fact block figures in the
  editorial section.

"Done when" for #234: an operator can publish a where or who card without
inventing evidence fields (the fact block has three fields and no evidence
part); the numeric stat contract is unchanged (pinned by
`functions/src/cms/blockTypes.test.cjs`); tests cover both block shapes
(`FactBlock.test.jsx`, `StatBlock.test.jsx`, `InfoCards.test.jsx`). Met in
full. The field type vocabulary is unchanged.

### Family 2: timeline, progress, notice bar, ruled table

- Contracts `timeline`, `progress`, `notice-bar`, `table` in
  `design/tokens/components.json`; every preset remaps each one where its
  story differs (Zine and Broadsheet thicken the spine; Atlas draws a station
  stop; Field Guide lengthens the tick; Zine and Broadsheet fill progress in
  ink; Civic, Broadsheet and Atlas take the full table grid; Atlas, Zine and
  Field Guide set the notice word and the table heads in the mono face).
- `apps/web/src/components/editorial/Timeline.jsx` (an ordered list on the
  spine the updates feed draws: `.update-feed__entry` now reads the timeline
  tokens), `editorial/RuledTable.jsx` (a real table inside
  `HorizontalScrollRegion`, sortable heads as buttons with `aria-sort`),
  `components/Progress.jsx` (native `<progress>` with the stated fraction),
  `components/NoticeBar.jsx` (`status` or `alert`, dismissed and remembered
  per browser by id). Tests beside each render in every style and both modes.
- Specimen book: timeline and ruled table in the editorial section (the table
  sorts live); notice bar and progress in the feedback section.
- `docs/design-reference.md`, `CHANGELOG.md`, and the purge test's class list.
- The notice bar has no content slot yet: what carries a notice into the
  shell is issue 196 and 199's work. The device, its contract and its
  specimen are ready for them.

### Family 3: dropzone, avatar, repeater

- Contracts `dropzone`, `avatar`, `repeater`. `components/forms/Dropzone.jsx`
  (a real file input inside a ruled region, `:focus-within` ring, progress
  through the progress device, a sent list with a state word),
  `components/editorial/Avatar.jsx` (`ProfilePhoto.jsx` now draws through it
  and keeps only the URL resolution), `components/forms/Repeater.jsx` (add
  and remove with focus following the change). Control specimens for the
  dropzone and the repeater account for all ten states; the avatar has a
  figure in the directories section.

### Family 4: count, figure sentence, legend, session state marker

- Contracts `count`, `figure-sentence`, `legend`, `state-marker`.
  `SavedCount` draws through `Count`; the schedule header's legend line
  draws through `Legend`; `SessionState` draws through `StateMarker`, so a
  finished session now takes the back-issue ink on the hairline and a running
  one the accent, as §3.2 asks. `FigureSentence` is the public form of the
  figure sentence §3.4 names; the admin pages consume it in wave 6.

### Family 5: standfirst, byline and dateline, drop cap

- Contracts `standfirst`, `byline`, `long-read`. `Standfirst` replaces the
  hand-written lead line in the specimen book; `Dateline` replaces the update
  detail page's date line; `LongReadOpening` wraps the first section of a
  page that states the Long read template (`ContentPage.jsx`), and the
  stylesheet draws the cap, the lead-sized line, or nothing on its first
  paragraph from the `long-read` tokens.

### Family 6: the option groups of §4

Every style carries five new groups, each remapping declared tokens only
(`design/tokens/presets/*.json`; the catalog mirrors regenerated):

| Group | Civic | Newsroom | Broadsheet | Atlas | Field Guide | Zine |
|---|---|---|---|---|---|---|
| Long read opening | plain | standfirst | drop cap | plain | drop cap | plain |
| Quote device | ruled minute, plain rules | ruled with mark, side rule, mark only | centred with mark, column rule | route line, boxed sign | field note, pressed page | handwritten, toner block, cut-out |
| Directory style | ruled entries, portrait plates | portrait shelf, tall portraits | agate directory, plate portraits | gazetteer, station index | specimen plates, field list | contact sheet, cut-outs |
| Section boundary | three | three | three | two (no margin folio) | three | two (no margin folio) |
| Table rules | full grid | ruled head | full grid (hairline head) | full grid | ruled head (hairline) | ruled head |

The defaults follow the record where it names one (Broadsheet and Field
Guide open on the cap, Zine and Atlas plain) and the style's own story where
it does not. The section boundary's "folio in the margin" knocks the folio
out of the rule where the margin column opens beside the measure; Zine's
struck folios and Atlas's grid have no room for it, so those two offer the
first two choices only. Two contracts were added so an option had tokens to
move: `directory` (the shelf's rule and portrait, the index's row) and two
folio tokens on `section-rule`. `resolveComponentFonts` now reads a picked
choice's `componentFonts`, so Zine's toner-block quote can drop the script
face; the catalog builder keeps that key on a choice.

### Family 7: the tabs and the segmented control take an unavailable state

Decision: both take `disabled` per item, drawn as `aria-disabled="true"`, not
`disabled`. The item stays in the row and in the arrow-key ring so a reader
can land on it and hear why (`hint` is read as part of its name); its handler
refuses every activation path (click, Enter, Space, and arrow selection), the
arrow keys move on from the focused item rather than from the selected one,
and both test files prove the refusal. The specimen book draws the state on
both controls. The speaker session hub (issue 212) can therefore show a tab
for a panel that exists and cannot be opened yet; a tab with nothing behind
it at all is still not rendered.

## Decisions the owner may want to change

- The `when` card is a fact too, not a stat. A date range is a term and a
  description; the previous stat asked for a source line for the dates. The
  demo's evidence-bearing `info__when` stat is replaced by the fact.
- A second block type, `quote`, landed with the pull quote so the device has
  a content slot. Its fields are `text` and an optional `attribution`.
- `SessionState` no longer draws through `Tag`. The record asks for a word with
  an ink change and a rule, and a finished session in the back-issue ink; the
  tag's boxed ground could not carry the ink change.
- `ProfilePhoto` lost its `rounded-brand` utility: the avatar's corner is the
  `--avatar-radius` token so a style can move it (Zine, Broadsheet and Field
  Guide square it). The default is still the brand radius.
- The preset catalog mirrors (`presetCatalog.cjs`, `presetRemaps.cjs`,
  `presetCopy.js`, `presets/README.md`) are committed with the source change,
  not in the regenerate commit: they are not on the director's regenerate
  list and their freshness test fails without them.
- A live `config/theme` overlay now paints one fetch later than it did. The
  provider waits for the remaps chunk (27KB raw, fetched in parallel with the
  config document) before it writes the runtime overlay, so a visitor sees
  the build-time look for that long. The alternative was carrying every
  style's remaps in the chunk every visitor downloads, for an overlay most
  deployments never write. A deployment that publishes its theme rebuilds,
  and the rebuilt stylesheet carries the style, so the wait is for the gap
  between a publish and the next deploy.

## Evidence

`docs/plans/evidence/specimen/`: four new section captures of the editorial
section (Broadsheet and Zine, light and dark, 1440px, full scale) and two
refreshed feedback captures (Broadsheet and Zine, light), 3.8MB in all, taken
from the committed demo build with the committed script. The README there
says what to look at. The directories and controls sections (the avatar, the
dropzone and the repeater) were not captured: a capture of either runs past
half a megabyte and the set had to stay small. Both are on the demo at
`#/specimen`.

## The bundle budget

The budget file (`scripts/ci/bundle-budget.json`: initial 1,080,000 raw and
292,000 gzip bytes) and the script are untouched. As first built, the wave
put the demo's initial chunk 7,622 raw bytes over. Most of the growth was the
preset catalog: with five option groups per style and the remaps every new
device needs, `presetCatalog.cjs` grew from 34.7KB to 60.2KB of source, and
all of it rode the chunk every visitor downloads, although a first paint
reads none of it (the generated stylesheet already carries the chosen style
with its picks resolved).

The catalog is now split by who needs it, on the director's route:

- `packages/shared/src/presetCatalog.cjs` (26.7KB of source, 14.9KB as data)
  keeps what every path reads: palettes, type maps, shape, motif default and
  each option group's default and choice ids. The config validator, the
  contrast check and the palette resolver in Cloud Functions read nothing
  else, so the functions bundle is unchanged in behaviour.
- `packages/shared/src/presetRemaps.cjs` (43.2KB of source, 27.4KB as data)
  carries what a style moves: its own token and component-font remaps, what
  each choice moves, and the component defaults a style change resets. It
  registers itself with the resolver when required. The web app loads it as
  its own lazy chunk (`apps/web/src/lib/presetRemaps.js`; 27,100 raw bytes
  in the demo, against a 180,000 lazy budget) on the paths that resolve a
  style at runtime: the provider's overlay effect waits for it before it
  writes `<style id="event-theme-runtime">`, so a live `config/theme` overlay
  still resolves a picked choice, in full or not at all; the admin chunk
  loads it alongside itself; the demo band prefetches it as it mounts so the
  first style switch lands as fast as the rest; the specimen book reaches it
  through the provider. Node callers reach it through
  `scripts/lib/shared-theme.cjs`, which requires it once, and the web test
  setup registers it for every test.
- `resolvePresetTokens`, `resolveComponentFonts` and `pickedChoices` read the
  registry and throw, naming the module, if asked before it is loaded, so a
  style is never half-resolved. A document that names no style resolves
  without it. `getPreset(id)` no longer carries `tokens` or `componentFonts`;
  the tests that read them read `PRESET_REMAPS.presets[id]` instead.
  `scripts/build-preset-catalog.test.cjs` checks that the two halves name
  the same styles and choices and that the component defaults cover exactly
  the tokens some style or choice moves.

Measured with `node scripts/ci/bundle-budget.cjs` on the builds this report
is committed with:

| Build | Initial raw | Margin | Initial gzip | Margin |
|---|---|---|---|---|
| `docs/demo` | 1,065,929 | 14,071 (1.3%) | 289,500 | 2,500 (0.9%) |
| `apps/web/dist` | 1,055,485 | 24,515 (2.3%) | 285,351 | 6,649 (2.3%) |

The base commit's demo (`a57b1d8`) measured 1,068,974 raw and 287,748 gzip,
so the wave leaves the initial chunk 3,045 raw bytes smaller than it found
it and 1,752 gzip bytes larger. What remains of the growth is the new devices
and blocks on the public pages, which are the work; nothing was trimmed to
fit. The demo's gzip margin is under one percent, so the next milestone that
adds public-page code should measure as it goes.

## Checks

Run last, after docs and generated output, on the tree this report is
committed with:

| Check | Result |
|---|---|
| `npm run lint` | pass |
| `npm test` | 2160 tests, 2160 pass, 0 fail |
| `flock … npm run test -w apps/web` | 202 files, 2192 tests: 2191 pass on the full run; the one failure was a 5.8s timeout in `AdminBranding.test.jsx` while the rules emulators and two builds ran beside it, and that file passes alone (33 tests, 1.2s for the same case) |
| `flock … npm run test:rules` | firestore 154 tests, storage 43 tests, all pass (a first attempt failed to bind port 8080 behind an emulator my own earlier attempt had left running; it was stopped and the run repeated) |
| `flock … npm run build -w apps/web` | built |
| `node scripts/ci/bundle-budget.cjs --dist apps/web/dist` | pass: 1,055,485 raw, 285,351 gzip |
| `node scripts/generate-content.cjs --demo --check` | 6 files match |
| `flock … node scripts/build-demo.cjs --check` | 141 files match |
| `node scripts/ci/bundle-budget.cjs --dist docs/demo` | pass: 1,065,929 raw, 289,500 gzip |
| `node scripts/build-pages.cjs --check` | 28 files and `docs/tokens.css` match |
| `npm run check:copy` | 396 files, no blocked pattern |

## Left undone

- The notice bar has no content slot; issues 196 and 199 wire it.
- The figure sentence has no admin call site; wave 6 wires it on the M9 to
  M11 pages.
- Full-page specimen captures were not regenerated (lesson of 2026-09-10).
