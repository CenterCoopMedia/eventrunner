# Builder b2 report: #183, the outbound email log

Branch `joe/confident-gates-gy99ip-b-b2`, worktree `/home/user/wt-b2`, cut after a4 (`426f430`).
Commits on top of the base, in order: `Add the outbound email log` (Closes #183), this report,
then `Regenerate generated output`.

## What changed

Server (`functions/src/email/log.cjs`, new). Two staff-tier POST endpoints. They follow the
`systemErrorsAdmin.cjs` shape: POST only, `applyCors`, the gate first, `core/errors.cjs`.

- `listSentEmails { q?, source?, status?, limit?, cursor? } → { rows, nextCursor, scanned }`.
  The query is `.select(...LIST_FIELDS)`, and `LIST_FIELDS` holds no `html` and no `text`, so a
  list never takes a body out of Firestore. With no `q`, one query reads `limit + 1` rows (at most
  51). With `q`, one query reads at most `SCAN_CAP = 500` rows. It keeps a row when `to` or
  `subject` contains `q` (case is ignored), and it stops at `limit` matches. `nextCursor` is the
  last row it examined. Each 400 names its field and never repeats the value. List requests write
  no `admin_logs` row.
- `getSentEmail { id } → { row }`: the list fields plus `html`, `text`, `providerMessageId` and
  `providerStatus`. A missing row answers 404. A successful read writes one `admin_logs` row:
  `action: 'view-sent-email'`, `docPath: 'sent_emails/<id>'`, the actor's uid and email, and `at`.
  The row holds no recipient address and no subject.
- `isValidDocId` checks both `id` and `cursor.id`, so a bad id is a 400 and never a thrown `doc()` or
  `startAfter()`.
- `firestore.indexes.json`: (source, sentAt desc), (status, sentAt desc), (source, status, sentAt
  desc) on `sent_emails`. `functions/index.js` and `.github/smoke-endpoints.json` each gain the two
  names.

Client:

- `apps/web/src/admin/emailPreview.js` (new): `buildPreviewDoc(html)`. It parses the stored body
  with DOMParser. It removes every `meta[http-equiv]`, `base` and `link`. It sets `target="_blank"`
  on every `a` and `area`. It then puts the policy meta, the `color-scheme: light` meta,
  `<base target="_blank">` and the canvas style first in `<head>`.
- `apps/web/src/admin/pages/AdminEmailLog.jsx` (new, lazy chunk of 13.46 kB): the page as the
  spec describes it. It has the title band with Refresh, the search form (`role="search"`), a
  Filters fieldset whose legend gives the active count, and the figure sentence. The ruled table
  sits in a focusable scroll region. The log row opens a preview frame. The page also has every
  empty, error, busy and focus state in spec §3.
- `AdminLayout.jsx`: one docket item under Operations, after Feedback:
  `{ to: 'email-log', label: 'Email log', tier: 'staff' }`. This is the one place a4 made for page
  tiers. `AdminApp.jsx` adds one lazy route.

Tests added:

- `functions/src/email/log.test.cjs`, 21 tests. It covers the gate (401, 403 for a non-admin, 403
  for an unverified address, 405), staff admitted, a fail-closed gate with no bootstrap, and the
  done line through the real `createEmailCore` and the real `auth.otp` render. It also covers the
  list fields and `select`, search, filters, 400s, paging ties, the 500-row window over 600 rows,
  limit stops, 500 on a failed query, the get's shape, the audit row, 404, the index declaration,
  and a drift alarm on the rules line.
- `tests/firestore.rules.test.js`, +1 test. It seeds a `sent_emails` row. A list query fails for
  admin, staff and non-admin, with and without a `where`. A get fails for staff and non-admin.
- `apps/web/src/admin/emailPreview.test.js`, 5 tests.
  `apps/web/src/admin/pages/AdminEmailLog.test.jsx`, 23 tests. `AdminApp.test.jsx` has +1 test:
  staff open `/admin/email-log`, and the page calls `listSentEmails`. Its three staff-list
  assertions now include Email log. `AdminLayout.test.jsx` asserts 17 links, not 16, and
  classifies `email-log` as staff.
- `e2e/email-log.spec.js` (new), 3 tests. `signIn` moved into `e2e/helpers.mjs`, with an
  `import { expect }`. `webmcp.spec.js` and a4's `admin-access.spec.js` now import `signIn` instead
  of keeping their own copies.

Docs: `docs/ADMIN_GUIDE.md` (the staff row, and a new "Email log" section after "Feedback"),
`functions/README.md` (the `src/email/` line, the staff "Runs" cell, the staff classification row),
`docs/interface-guidelines.md` (one bullet in "The admin surface"), `CHANGELOG.md` (one Added
entry), `docs/ROADMAP.md` (#183 ticked).

## Done when: met in full, on the real surface

> "a sent code email appears in the log"

`e2e/email-log.spec.js` › "a sent code email appears in the log, with its body not stored". The
seeded operator signs in through the real sign-in page, which sends a real OTP through the console
provider and `send.cjs`. On `/admin/email-log` the test types the operator's address into the
search field and submits. A row with the address and "Sign-in code" shows "Not stored", and its
preview gives the not-stored reason with no frame. Unit test: `log.test.cjs` › "a code email sent
through the real email core appears in the log…" sends the shipped `auth.otp` render through
`createEmailCore` into the same fake db the reader lists from.

> "a non admin caller is refused by the endpoint"

`e2e/email-log.spec.js` › "the endpoint refuses a caller with no token and a signed-in non-admin".
Against the emulated functions, `listSentEmails` with no token answers 401. `listSentEmails` and
`getSentEmail` with a signed-in non-admin's token both answer 403. Unit test: `log.test.cjs` ›
"both endpoints: 401 with no token, 403 for a non-admin and an unverified admin address, 405 for
GET".

> "the rules are unchanged"

`git diff 426f430 HEAD -- firestore.rules` is empty. The proof on the emulator is
`tests/firestore.rules.test.js` › "denies a list query on sent_emails to admin, staff and
non-admin, and a get to staff and non-admin", together with the existing "denies all access to
sent_emails, admin included". The pin in `log.test.cjs` is a drift alarm only.

> "the preview cannot run script"

`e2e/email-log.spec.js` › "the preview renders a stored body with no script and no request". This
runs in real Chromium on the shipped page. A seeded body holds a `<script>`, an `<img onerror>`
that each rewrite `#probe`, a remote image and a link. After Preview:

- The frame's `sandbox` attribute is `""` and `referrerpolicy` is `no-referrer`.
- The `srcdoc` holds the exact policy meta.
- `#probe` in the frame still reads "Script did not run.", also after the link is clicked.
- No popup opens.
- Each request the frame raised fails with the reason `csp`, and none gets a response.
- The link raised no request.
- The page made exactly one `getSentEmail` POST.
- Chromium logs "Blocked script execution in 'about:srcdoc' because the document's frame is
  sandboxed…", "Refused to load the image 'https://pixel.example.test/open.png'…" and "Blocked
  opening 'https://pixel.example.test/read' in a new window…".
- The `admin_logs` row exists and does not hold the address.

The web tests check the real frame attributes and parse the real `srcdoc`
(`AdminEmailLog.test.jsx` › "opens a preview in an empty sandbox under the content policy…").

Mutation runs, made and reverted before the final run, show that each barrier holds by itself:

- **Policy meta removed:** the image requests failed with a network error instead of `csp`, and
  the spec failed.
- **Sandbox set to `allow-scripts`:** the policy alone still refused the inline script ("Refused
  to execute inline script because it violates … default-src 'none'"). The spec failed on the
  missing sandbox message.

### What a browser does with each frame attribute and each directive

| Item | What the browser does |
|---|---|
| `sandbox=""` | Sets every sandbox flag. The document gets an opaque origin, so it cannot read the admin page, its storage or its token. Script is off: `<script>`, inline handlers and `javascript:` URLs do not run. Forms do not submit. Popups and new windows are refused, so a `target="_blank"` link does nothing. The frame cannot navigate the top page. Plugins, modals and downloads are off. The frame can still navigate itself, which is why every link is sent to `_blank`. |
| `srcdoc` | The frame's document comes inline, as `about:srcdoc`. Loading the frame fetches nothing. |
| `referrerpolicy="no-referrer"` | A request from the frame element carries no `Referer`. There is no such request with `srcdoc`, so this attribute is a spare barrier. |
| `title` | The frame's accessible name, "Message to {to}". |
| `[color-scheme:light]` (class) | Sets the frame element's used colour scheme to light, so `prefers-color-scheme` inside the frame reads light (see Departures). |
| Policy meta first in `<head>` | A meta policy governs only what the parser meets after it, so it comes first. |
| `default-src 'none'` | This is the fallback for script, connect, font, frame, media, object, manifest and worker. The browser refuses every fetch of those kinds. It also refuses every script, inline or remote, because the policy has no `'unsafe-inline'` for script. |
| `img-src data:` | Only `data:` images load. Remote images, relative images, `srcset` and CSS `url()` backgrounds are refused before the request leaves the renderer, so a tracking pixel learns nothing. |
| `style-src 'unsafe-inline'` | Inline `<style>` and `style=""` apply, and mail depends on them. An external stylesheet or `@import` is refused. |
| `form-action 'none'` | A form cannot submit anywhere. This is the second barrier after the sandbox. |
| `base-uri 'none'` | Any `<base href>` is ignored. This is the second barrier after the DOMParser pass removes stored `base` elements. |

## Departures from the spec, where the code proved it wrong or short

1. **Every `a` and `area` gets `target="_blank"`, not only `a[href]` and `area[href]`.** An SVG
   `<a xlink:href>` does not match `a[href]`. A Chromium probe showed that a `target="_self"` link
   in the empty sandbox navigates the frame and makes a real network request. A `_blank` link,
   HTML or SVG, is refused ("Blocked opening … 'allow-popups' permission is not set"). This closes
   the "SVG link navigates the frame" residual risk in spec §4. `emailPreview.test.js` covers the
   SVG anchor.
2. **The E2E network proof reads each request's outcome, not the bare request list.** Spec §5
   says that no request names `pixel.example.test`. In Chromium, `page.on('request')` does see a
   request that the content policy refuses: it fires, and then `requestfailed` fires with the
   reason `csp`, and nothing leaves the renderer. The first run failed on that assertion. The spec
   now asserts that every such request fails with `csp`, that none gets a response, and that the
   link raises no request at all.
3. **`[color-scheme:light]` on the iframe element.** Inside a frame, `prefers-color-scheme`
   follows the embedding element's used colour scheme. The admin sets none, so without this class
   a mail's own dark-mode rules could switch on under a dark system setting while the preview draws
   the light canvas. This supports spec §3's "a light mail client … in both admin modes".
4. **Subject cell.** "Not stored" shows when `bodyStored` is false. "No subject" shows when a body
   was stored but the subject is null, for example a failed send with no recipient. The spec said
   "(or 'Not stored')" for both.
5. **The empty search-window state counts what was examined.** Its heading reads "No match in the
   500 most recent messages" after one window and "…1,000…" after the next, and the pager reads
   "Search older messages". The "read at" time in the figure sentence uses the event clock and
   zone label, the same as the Sent column, for example "read at 10:13 PM UTC".
6. **Privacy additions.** The search field has `autocomplete="off"`, so a searched address does
   not go into the browser's form history. A 400 never repeats the value it refused. A 403 hides
   the form as well as the table.
7. **`getSentEmail` writes its audit row only on a successful read.** A 400, 403 or 404 writes
   none, because no mail was read.
8. **The shared `signIn` has three importers, not two.** a4 added a third copy in
   `admin-access.spec.js` after the spec was written, and that spec now imports the shared helper
   too.

## Decision 10: deviations from the design record, for the director to accept

- **No sort control**, though design record §8 lists #183 under the sort control. The server
  order is newest first. A client-side sort of one page would mislead, because it would reorder
  only the rows loaded.
- **No bulk-action row** (design record §3.4). The log has no action to apply to a row.
- **The search text stays out of the URL**, though design record §3.3 keeps a query there. An
  address in the URL reaches client error reports: `lib/errorReporting.js` sends
  `window.location.href`, and `telemetry/redact.cjs` misses a `%40`-encoded address. Source and
  status do stay in the URL.

## Decisions the owner may want to change

- Staff see every row, operator alerts included (open question 1). An operator-only carve-out
  would need a different query and index.
- The Sent column and "read at" use the event clock with its zone label. With no `timezone`, or an
  invalid one, they use the reader's clock and its label (open question 2).
- Search is a bounded scan of 500 rows per request with no new field, and status filters the
  stored `sent`/`failed` only (decisions 1 and 2). Delivery state shows as a word on the row.
- The page, both endpoints and the docket entry are staff tier (decision 5).

## Follow-up issues for the director to file

1. **`telemetry/redact.cjs` misses a percent-encoded address.** `EMAIL_RE` needs a literal `@`, so
   `%40` survives into `system_errors.url`, and `systemErrors.cjs` forwards it to the operator
   sink. Fix: decode the query string before redaction (open question 5). #183 avoids the path by
   keeping the search out of the URL.
2. **Retention for `sent_emails`.** Nothing deletes rows. Stored bodies can hold personal data
   (open question 3).
3. Optional: an indexed exact-address lookup for a search over all history (open question 4).

## Final check results (last run, after docs and generated output)

Each check below ran after the last edit to any file it reads. Order: the feature commit, then
`generate-content --demo`, `build-demo` and `build-pages`, then the whole set. Counts are stated
against the base.

| Check | Result |
|---|---|
| `npm run lint` | clean, exit 0 |
| `npm test` (shared, functions, scripts) | 2287 tests, 2287 pass, 0 fail (+21 from `log.test.cjs`) |
| `flock … npm run test -w apps/web` | 188 files, 2174 tests, all pass (+2 files, +29 tests: 5 preview, 23 page, 1 route) |
| `flock … npm run test:rules` | 2 files, 283 tests, all pass (+1) |
| `flock … npm run build -w apps/web` | built, exit 0. Main chunk 1,059.06 kB / 285.97 kB gzip; `AdminApp` 173.25 kB; `AdminEmailLog` 13.46 kB (lazy) |
| `node scripts/ci/bundle-budget.cjs --dist apps/web/dist` | pass: 1 initial chunk, 1,059,107 raw / 285,385 gzip bytes |
| `node scripts/generate-content.cjs --demo --check` | 6 files match the committed snapshot |
| `flock … node scripts/build-demo.cjs --check` | `docs/demo` matches a fresh build (140 files) |
| `node scripts/ci/bundle-budget.cjs --dist docs/demo` | pass: 1 initial chunk, 1,069,270 raw / 289,319 gzip bytes |
| `node scripts/build-pages.cjs --check` | 29 files and `docs/tokens.css` match |
| `npm run check:copy` | 381 files, no blocked patterns |
| `flock … npm run test:e2e` | 20 passed, 0 failed (+3 in `email-log.spec.js`) |

The `Regenerate generated output` commit carries `docs/docs/**` and `docs/demo/**`. It also
carries the two lockfile integrity lines. The worktree already had them when it was handed over:
they come from a4's `packages/shared` change, whose regenerate commit was dropped. b2 changes
nothing under `packages/shared`.

## Shared files touched (small and additive, for the merge with b1 and b3)

- `AdminLayout.jsx`: one docket item, and the rail comment's count ("Sixteen" becomes "Seventeen").
- `AdminApp.jsx`: one lazy import and one route.
- `AdminLayout.test.jsx`: 16 becomes 17, one staff id, and one `sectionTier` line.
- `AdminApp.test.jsx`: "Email log" in three lists, and the refusal string, which is derived from
  `TIER_SCOPE`.
- `functions/index.js`: one require and one spread. `.github/smoke-endpoints.json`: two names
  after `emailDeliveryWebhook`.
- `firestore.indexes.json`: three entries before `speaker_invites`.
- `CHANGELOG.md`: one entry at the top of Added. `docs/ADMIN_GUIDE.md`: the staff row and one
  section. `functions/README.md`: three cells.
- `e2e/helpers.mjs`: `signIn` appended, and `expect` imported.

If b1 and b3 also add docket items, the count in the comment and in the test is the sum.
`TIER_SCOPE.staff`, the refusal string in `AdminApp.test.jsx` and the staff row in
`ADMIN_GUIDE.md` then list every new staff section.

## Left undone

Nothing from #183. The two follow-up issues above are outside its scope.
