# apps/web

Vite frontend. Not landed yet. Tracked under milestone M2 (pages-as-data, CMS, auth, schedule, theming).

Do not add a hardcoded event string or hex color here when the app arrives. Event identity comes from `packages/shared` config.
